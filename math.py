
# I have to do 

def add(x, y):
  # if the type of x is int and 
  # if the type of y is also int 
  # then return (x + y)
  # else return None
  if ((type(x) == int) and (type(y) == int)):
    return (x + y)
  else:
    return (None)

# Niranjan have to do
def mul(x, y):
  return None

print("HEllo")
